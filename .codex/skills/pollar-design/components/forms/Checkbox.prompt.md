One-line: selection control for table rows, filter lists and consent — supports an indeterminate header state.

```jsx
<Checkbox label="Selecionar todos" indeterminate onChange={toggleAll} />
```

Intentional addition — required by the row-selection behavior in §9 Tables.
