One-line: single-choice dropdown matching Input geometry — used for account, category, period and column pickers.

```jsx
<Select label="Conta" options={["Conta corrente", "Poupança", "Cartão Ouro"]} />
```

Intentional addition (not enumerated in the source spec) — required by the filter toolbars and column selection described in §9 Tables.
