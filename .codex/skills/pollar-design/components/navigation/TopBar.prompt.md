One-line: the desktop utility bar — page identity on the left, global search (⌘K) and utility actions on the right.

```jsx
<TopBar breadcrumb="Cartões" title="Cartão Ouro" actions={<><IconButton icon="eye-off" label="Modo privacidade"/><Button iconLeft="plus" size="compact">Nova transação</Button></>} />
```

The primary page action belongs in this header. Keep shortcuts for quick add, global search and privacy mode.
