One-line: grouped bars for income vs. expenses or budget vs. actual — zero baseline, legend and per-bar exact values.

```jsx
<ComparisonBars
  series={[{label:'Entradas',color:'var(--chart-1)'},{label:'Saídas',color:'var(--chart-3)'}]}
  groups={[{label:'jul',values:[980000,742000]},{label:'ago',values:[1010000,861000]}]} />
```
