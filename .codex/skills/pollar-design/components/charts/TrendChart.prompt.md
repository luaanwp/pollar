One-line: the cash-flow / balance-over-time chart; carries axis labels and a textual last-value caption as the non-visual alternative.

```jsx
<TrendChart data={[{label:'abr',value:1420000},{label:'mai',value:1512000},{label:'jun',value:1487000}]} />
```

Zero baseline is always drawn. Never animate the value in a way that delays comprehension.
