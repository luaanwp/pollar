One-line: category-composition donut that always ships its own value table as the text alternative.

```jsx
<CategoryDonut centerLabel="Saídas" centerValue="R$ 8.610,00"
  slices={[{label:'Moradia',value:-320000},{label:'Alimentação',value:-138050}]} />
```

Only for few categories; with many, switch to sorted bars.
